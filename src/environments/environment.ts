import { Environment } from "./environment.d";

export const ENV: Environment = {
  production: true,
  mode: "Production",
  isDebugMode: true,
  firebase: {},
  interface: {
    showMenu: true,
    showHeader: true
  }
};
